import { InvalidStateErrorFactory } from '../factories/invalid-state-error';
import { TUnpatchedAudioContext } from '../types';
export declare class ChannelMergerNodeWrapper {
    private _invalidStateErrorFactory;
    constructor(_invalidStateErrorFactory: InvalidStateErrorFactory);
    wrap(audioContext: TUnpatchedAudioContext, channelMergerNode: ChannelMergerNode): void;
}
