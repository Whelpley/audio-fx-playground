import { IndexSizeErrorFactory } from '../factories/index-size-error';
export declare class AudioBufferWrapper {
    private _indexSizeErrorFactory;
    constructor(_indexSizeErrorFactory: IndexSizeErrorFactory);
    wrap(audioBuffer: AudioBuffer): void;
}
