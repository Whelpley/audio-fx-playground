import { InvalidAccessErrorFactory } from '../factories/invalid-access-error';
import { IAudioNode } from '../interfaces';
export declare class AudioNodeConnectMethodWrapper {
    private _invalidAccessErrorFactory;
    constructor(_invalidAccessErrorFactory: InvalidAccessErrorFactory);
    wrap(audioNode: IAudioNode, isSupportingChaining: boolean): void;
}
